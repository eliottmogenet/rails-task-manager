
<h1>My tasks</h1>

<% @tasks.each do |task| %>
<ul>
  <li> [ ] <%= link_to task.title, task_path(task.id) %> <%= link_to "Delete",
            task_path(task.id),
            method: :delete,
            data: { confirm: "Are you sure?" } %> </li>
</ul>
<% end %>

<hr>

<%= link_to "+ add a new task", new_path %>

